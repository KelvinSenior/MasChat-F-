// config.ts
// This config now uses the same BASE_URL as your login screen

export const apiUrl = 'http://10.132.74.85:8080/api';

export default {
  API_URL: apiUrl,
  ENV: 'production',
  IS_DEV: false,
};
